import React from 'react';

const Button = ({props}) => {
    console.log('This is button section', props)
    return (
        <div>
            <button></button>
        </div>
    );
};

export default Button;